# sample hyprlock.conf
# for more configuration options, refer https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock
#
# rendered text in all widgets supports pango markup (e.g. <b> or <i> tags)
# ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#general-remarks
#
# shortcuts to clear password buffer: ESC, Ctrl+U, Ctrl+Backspace
#
# you can get started by copying this config to ~/.config/hypr/hyprlock.conf
#

$font = JetBrainsMonoNL Nerd Font Mono

general {
    hide_cursor = false
}

# uncomment to enable fingerprint authentication
# auth {
#     fingerprint {
#         enabled = true
#         ready_message = Scan fingerprint to unlock
#         present_message = Scanning...
#         retry_delay = 250 # in milliseconds
#     }
# }

animations {
    enabled = true
    bezier = linear, 1, 1, 0, 0
    animation = fadeIn, 1, 5, linear
    animation = fadeOut, 1, 5, linear
    animation = inputFieldDots, 1, 2, linear
}

background {
    monitor =
    path = /home/luka/Pictures/wallpapers/wallhaven-kx7pe1.png
    blur_passes = 2
}

input-field {
    monitor =
    size = 13%, 4%
    outline_thickness = 1
    inner_color = rgba(0d1011b4)
    outer_color = rgba(0d1011b4)
    # outer_color = rgba(a9b1d6ff)
    # check_color = rgba(2a2f4164)
    check_color = rgba(a9b1d6ff)
    fail_color=rgba(d16983ff)

    # font_color = rgb(143, 143, 143)
    font_color = rgb(169, 177, 214)
    fade_on_empty = false
    rounding = 5

    font_family = JetBrainsMonoNL Nerd Font Mono
    placeholder_text = Input password...
    fail_text = $PAMFAIL

    # uncomment to use a letter instead of a dot to indicate the typed password
    # dots_text_format = *
    # dots_size = 0.4
    dots_spacing = 0.3

    # uncomment to use an input indicator that does not show the password length (similar to swaylock's input indicator)
    # hide_input = true

    position = 0, -100
    halign = center
    valign = center
}
label {
    monitor =
    text = Hello there, $USER :)
    color = rgba(a9b1d6ff)
    font_size = 25
    font_family = JetBrainsMonoNL Nerd Font Mono

    position = 0, 0
    halign = center
    valign = center
}
# TIME
label {
    monitor =
    text = $TIME # ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#variable-substitution
    color = rgba(a9b1d6ff)
    font_size = 90
    font_family = $font

    position = 0, 150
    halign = center
    valign = center
}

# # DATE
# label {
#     monitor =
#     text = cmd[update:60000] date +"%A, %d %B %Y" # update every 60 seconds
#     color = rgba(a9b1d6ff)
#     font_size = 25
#     font_family = $font
#
#     position = 0, 150
#     halign = center
#     valign = center
# }

label {
    monitor = 
    text = Me want to work when me want to work. Most of the time me want to just chill or whatever, or just hang with me beetches.
    color = rgba(a9b1d6ff)
    font_size = 15
    font_family = Liberation serif

    position = 0, 20
    halign = center
    valign = bottom
}
